[project]
name = "tx-risk-agent"
version = "0.1.0"
description = "Reproducible SignShield EVM pre-signature risk analysis prototype."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "requests>=2.32",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
